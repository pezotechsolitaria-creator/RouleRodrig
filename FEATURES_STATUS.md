# Roule Rodrigues — Feature Status

> _Last generated by an automated audit on 2026-06-22 — verify against code before relying on any single claim._

A per-feature status reference for Roule Rodrigues (live at **roulerodrig.com**). It reorganizes the product feature catalogue into grouped tables, each row carrying a status drawn from the audit of shipped code plus the development-history record. For the architecture behind these features see [ARCHITECTURE.md](./ARCHITECTURE.md); for the data model see [DATABASE_SCHEMA.md](./DATABASE_SCHEMA.md); for the backlog see [NEXT_STEPS.md](./NEXT_STEPS.md).

## Status legend

| Symbol | Meaning |
|---|---|
| ✅ | **Live** — shipped and routed in production |
| ✅ (content-gated) | **Live**, but only renders once the owner adds the backing data in `/admin` |
| 🟡 | **Partial** — foundation/subset shipped, more in roadmap |
| ⏳ | **Planned** — specified, not yet built |
| ❌ (Infra-only) | **Not built** as customer-facing UI — DB/schema exists, no surface yet |

---

## 1. Island guide / SEO content

SEO is treated as a real acquisition channel: every page sets `metadata`, emits JSON-LD (`lib/schema.ts`, `components/JsonLd.tsx`), and uses ISR (`export const revalidate`).

| Feature | Route(s) / file | Status |
|---|---|---|
| Rodrigues island guide (15 knowledge topics, FAQPage schema) | `/guide/rodrigues` (`app/guide/rodrigues/page.tsx`, `lib/rodrigues-knowledge.ts`) | ✅ |
| Best beaches guide | `/guide/beaches` | ✅ (content-gated: only beaches with a written `story`) |
| Viewpoints & landmarks | `/guide/viewpoints` | ✅ |
| Scooter routes & hiking trails (real distance/elevation/difficulty) | `/guide/routes` | ✅ |
| Shopping directory | `/guide/shops` | ✅ (content-gated — 404s / `noindex` until a `category:"shop"` map location exists) |
| Travel blog | `/blog`, `/blog/[slug]` (`lib/blog.ts`, `BLOG_POSTS`) | ✅ (1 published post at audit: `how-many-days-in-rodrigues`) |
| Explore hub (beaches/viewpoints/hikes/rides/tours aggregated) | `/explore` (`components/ExploreClient.tsx`) | ✅ |
| Interactive island map (Leaflet) | `/map` (`components/MapSection.tsx`, `IslandMap.tsx`) | ✅ |
| Trip planner (auto day-by-day itinerary) | `/trip-planner`, `POST /api/trip-planner` | ✅ (rule-based sequencing over admin-editable place pool) |
| FAQ | `/faq` | ✅ |
| Emergency / useful numbers | `/emergency` (`components/UsefulNumbers.tsx`) | ✅ |
| French SEO landing pages (server-rendered real French + hreflang) | `/fr/location-scooter-rodrigues`, `/fr/plages-rodrigues`, `/fr/guide-rodrigues` | ✅ |
| "More" hub + legal docs | `/more`, `/legal/{terms,privacy,refunds,disclaimer,owner-agreement}` | ✅ |
| Sitemap / robots / manifest (PWA) | `app/sitemap.ts`, `app/robots.ts`, `app/manifest.ts` | ✅ |
| Per-place `/guide/[slug]` pages (SEO Wave 2) | `tasks/todo.md` | ⏳ (21 island-guide stories still locked in a client-side map component; no indexable pages) |

---

## 2. Vehicle rentals (revenue core)

Stated success metric (`PRODUCT.md`): **"Success today = more rental bookings."** Scooter and car rentals are the core revenue; everything else funnels a visitor toward a paid booking.

| Feature | Route / file | Status |
|---|---|---|
| Home hub (fleet rails, quick actions, promo carousel) | `/` (`components/AppHome.tsx`, `Hero.tsx`) | ✅ |
| Browse by category (scooter, car, stays, activities, tours, events…) | `/browse/[category]` (`components/BrowseTabs.tsx`, `Fleet.tsx`) | ✅ |
| Live availability calendar + date-based booking | `components/AvailabilityCalendar.tsx`, `POST /api/availability`, `POST /api/bookings` | ✅ |
| Deposit payment (PayPal EUR) | `POST /api/paypal/create-order`, `/api/paypal/capture-order`, `components/PayPalDeposit.tsx` | ✅ (returns 503 locally by design — creds only in Vercel) |
| Booking management, no account (ref `RR-XXXXXX`) | `/manage-booking`, `POST /api/bookings/lookup` | ✅ |
| Hold/expiry logic (first-to-pay wins; loser notified) | `lib/holds.ts`, `HOLD_EXPIRY_HOURS`, `POST /api/cron/reminders` | ✅ |
| Supplier acquisition / listing | `/list-your-scooter`, `/partner`, `/api/owner-apply`, `/api/leads` | ✅ |

---

## 3. Stay · Eat · Do bookings (places)

| Feature | Route / file | Status |
|---|---|---|
| Stays / activities / tours listings (`place_bookings` flow) | `/browse/stays`, `/browse/activities`, `/browse/tours`, `POST /api/place-bookings`, `/api/place-availability` | ✅ (content-gated) |
| Place detail + booking modal (owner-set flat deposit) | `components/PlaceBookingModal.tsx`, `PlaceDetailModal.tsx`, `RecommendedPlaces.tsx` | ✅ |
| WhatsApp **food concierge** (books your table via chat) | `/food` (`components/FoodConcierge.tsx`) | ✅ — WhatsApp handoff, not a restaurant listing |

> Note (owner-confirmed): the "Food & Dining" hub tile is a **WhatsApp concierge at `/food`**, not a restaurant listing (`/browse/restaurants` redirects to `/food`). Food & events live in `/explore`.

---

## 4. Merchant marketplace (Local Store)

**Business model = subscription, not commission** (canonical). Marketplace product payments = **cash / bank transfer / manual only**; cards are rentals-only.

| Feature | Route / file | Status |
|---|---|---|
| Merchant auth (email+password / Google, admin-approval gated) | `/merchant/login`, `/auth/callback`, `lib/supabase/*` | ✅ |
| Merchant onboarding (one shop per owner) | `/merchant/onboarding`, `POST /api/merchant/onboard`, `onboard_merchant()` RPC | ✅ |
| Merchant dashboard (stats, low-stock, order count) | `/merchant` (`lib/merchant/context.ts`) | ✅ |
| Product & variant management + media upload/reorder | `/merchant/products`, `/products/new`, `/products/[id]/edit`, `/api/merchant/products`, `/api/merchant/media/*` | ✅ |
| Orders list + detail + manual payment confirmation | `/merchant/orders`, `/orders/[id]`, `/api/merchant/orders/[id]/confirm-payment` | ✅ |
| Store hours / schedule + delivery window | `/merchant/hours`, `set_store_hours` RPC, `lib/schedule.ts` | ✅ |
| Payment settings (cash / bank transfer; read-only RR delivery fees) | `/merchant/payments`, `/api/merchant/payment-settings` | ✅ |
| Subscription status + billing history | `/merchant/subscription`, `merchant_subscriptions`, `subscription_invoices` | ✅ (manual billing) |
| Customer storefront (browse store, product, cart, checkout, orders) | `/shop/[storeSlug]`, `/shop/[storeSlug]/[productSlug]`, `/cart`, `/checkout`, `/orders`, `/orders/[id]` | 🟡 (Phase 2 — checkout requires login; `place_order`/`create_order` RPC live) |
| Payment methods for product orders | `create_order` RPC — **cash / bank transfer / manual only**; cards rejected (`20260805091000_create_order_reject_card_providers.sql`), MCB Juice removed (`20260805115000`) | ✅ (live rule) |
| Delivery: pickup / customer_delivery / **rr_delivery** (zone-priced) | `20260805105000`, `/admin/delivery-zones`, `/api/delivery-zones`, `lib/orders/location.ts` | ✅ |
| QR single-use pickup token (HMAC, screenshot-proof) | `qr_pickup_tokens` table, `MARKETPLACE.md` §QR pickup | ❌ (Infra-only — customer redeem endpoint `/api/marketplace/qr/redeem` is roadmap) |
| Coupons / reviews (product) | `coupons`, `reviews` tables (`20260730000001_marketplace_core.sql`) | ❌ (Infra-only / Phase 2) |
| Recurring subscription auto-billing | `subscription_invoices` (recorded manually today) | ⏳ (no automated biller; admin enters invoices) |
| Payout / settlement dashboard | `MARKETPLACE.md` Phase 2 | ⏳ (money never touches the platform; no online capture on marketplace side) |

> **Doc-vs-code drift:** `MARKETPLACE.md` specifies storefront routes `/store`, `/store/[slug]`, `/product/[id]` and admin `/admin/marketplace/**`; the shipped code uses **`/shop/[storeSlug]/[productSlug]`** and admin marketplace management lives at **`/admin/subscriptions`, `/admin/stores`, `/admin/delivery-zones`** (plus `/api/admin/marketplace`). The doc is the target architecture, not the current route map. *(To confirm whether `/store` will be aliased.)*

---

## 5. Taxi / transport module

| Feature | Route / file | Status |
|---|---|---|
| Public taxi-driver directory (featured-first, aggregated ratings) | `/taxi`, `GET /api/taxi` (tables `taxi_drivers`, `taxi_driver_reviews`) | ✅ (content-gated on active drivers) |
| Customer taxi reviews (moderated, `status:"approved"`) | `POST /api/taxi/reviews`, `/api/admin/taxi-reviews` | ✅ |
| "Getting Around" transport options (bus/bike/etc.) | `components/GettingAround.tsx`, admin `gettingAround` section | ✅ (content-gated) |

> Homepage note (owner-confirmed): "Getting Around" was **retired from the homepage** in favour of a Taxi action; the transport-options content still exists but is surfaced elsewhere, not as a home rail.

---

## 6. Ti Roulé assistant

| Aspect | Detail | Status |
|---|---|---|
| Site-wide AI island guide | `components/GlobalTiRoule.tsx` → `TiRouleGuide.tsx`; mounted once, hidden on `/admin`, `/v2`, `/merchant` | ✅ |
| Answer engine | **Local rule-based, no LLM** — `matchKnowledge()` over `lib/rodrigues-knowledge.ts`; "zero LLM/API cost… nothing to bill and nothing to hallucinate." Only external call is live FX via `/api/rates` | ✅ |
| Trilingual replies (en/fr/cr) + human WhatsApp handoff | logs unmatched questions to `/api/leads` (fire-and-forget, no PII) | ✅ |
| Animated mascot (15 poses, owner-uploaded) | `lib/mascot.ts` `MASCOT_POSES`, `resolvePose()`, admin Branding section | ✅ |

> **Owner-confirmed brand rule:** the Ti Roulé mascot artwork is **owner-supplied and must NEVER be redrawn** — animate it, never regenerate it.

---

## 7. Trilingual (en / fr / cr)

| Aspect | Detail | Status |
|---|---|---|
| Trilingual UI + content (Creole first-class) | `useLanguage()` (`context/LanguageContext`) + `loc(lang, base, fr, cr)` (`lib/localize.ts`); content fields carry `*Fr`/`*Cr` siblings (`lib/i18n.ts`, 1267-line dict) | ✅ (hard product constraint) |
| Server-rendered French SEO pages + hreflang | `/fr/*` pages exist because Google never saw the client-side French | ✅ |

---

## 8. Admin panel

| Aspect | Detail | Status |
|---|---|---|
| Auth | Cookie session `rr_admin` (`COOKIE_NAME`, `lib/auth.ts` `verifySession`), gated in `middleware.ts`; issued by `/admin/login` against `ADMIN_PASSWORD`, signed with `SESSION_SECRET` | ✅ |
| Dashboard | `app/admin/AdminDashboard.tsx` (~6,418 lines) — sections (`NAV`): **Overview** (Dashboard, Bookings, Stay & Activity Bookings, Enquiries, Customer Reviews, Waitlist, Alerts & Email), **Explore** (Vehicles, Food Concierge, Accommodations & Activities, Getting Around, Events, Taxi & Transport), **Content** (Hero, Island Guide, Trip Planner, Ride Routes, Home Cards, Home Tiles, Useful Numbers, FAQ, Sponsors/Partners, Contact Info, Branding & Social) | ✅ |
| Marketplace admin | `/admin/subscriptions` (Merchants & Subscriptions), `/admin/stores` (Shops & Opening Hours), `/admin/delivery-zones` (Delivery Areas & Fees) | ✅ |
| Supporting APIs | `/api/admin/*` (bookings, content, email, gallery, leads, notifications, owner-applications, partners, place-bookings, reviews, stores, submissions, subscriptions, taxi, upload, waitlist) | ✅ |
| Notifications to owner | Email via **Brevo** (or Resend), WhatsApp alerts via **CallMeBot** (`lib/email.ts`, `lib/notifications`, `lib/whatsapp.ts`) | ✅ |
| Home tiles/cards fully editable (add/remove/reorder/rename/link/photos) | admin Content sections | ✅ |

---

## 9. PWA / installable app

| Aspect | Detail | Status |
|---|---|---|
| Installable PWA + manifest | `app/manifest.ts` — `display: standalone`, portrait, `start_url: /?source=pwa`, 192/512 icons | ✅ |
| Service worker (offline shell, network-first HTML) | `public/sw.js` (`rr-cache-v94`); `/admin` and `/api` never cached | ✅ |
| Installed-app UX (splash gate, iOS launch images, Android install prompt capture) | `app/layout.tsx`, `components/PWARegister.tsx` | ✅ |

---

## What is deliberately not built yet

The following are specified (in `MARKETPLACE.md` / roadmap) or have DB infra, but are intentionally **not** customer-facing today:

- ⏳ **Recurring subscription auto-billing** — `subscription_invoices` are entered manually by the admin; no automated biller.
- ⏳ **Payout / settlement dashboard** — money never touches the platform; merchants confirm cash/bank-transfer receipts manually, so there is nothing to settle.
- ❌ **Signed single-use QR pickup redemption** — `qr_pickup_tokens` table exists, but no redeem route/UI is wired (`/api/marketplace/qr/redeem` does not exist); today "QR" is a merchant-supplied static image (`store_payment_settings.qr_image_url`).
- ❌ **Product coupons and marketplace reviews** — `coupons` and `reviews` tables exist (Infra-only / Phase 2), no customer UI.
- ⏳ **SEO Wave 2 `/guide/[slug]` per-place pages** — the 21 island stories are still in a client-side component with no indexable pages (blocked on real GSC/Ahrefs/GA4 data per `tasks/lessons.md`).
- ❌ **Online card capture on the marketplace side** — MCB Juice was built then removed; cards stay rentals-only.
- ⏳ **`MARKETPLACE.md` "Phase 3" intelligence/scale** — AI recommendations, restock forecasting, AI product descriptions, review moderation, local card rails, multi-island tenancy.
- **Explicitly deferred (with rationale in `MARKETPLACE.md`):** delivery-fleet dashboard (the model is QR pickup), microservices (a modular monolith is correct at this scale), and a full 150-feature super-admin.

**Not production data:** `content.json` at the repo root is a stale/sample export (placeholder fleet item "Ninja kawa", `Rs 0` prices, fabricated-looking testimonials). The live source of truth is Supabase `site_content`, **not** this file.

See [NEXT_STEPS.md](./NEXT_STEPS.md) for the prioritized roadmap and the owner-only actions these gaps imply.
